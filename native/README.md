# Native Layer

Native integration modules live here. `:native:bridge` owns the Android Gradle integration for Rust, Cargo, UniFFI binding generation, and Kotlin-friendly FFI adapters. The current FFI crate lives at `rust/crates/ffi_entry`.
